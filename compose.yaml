services:
  website:
    build:
      context: .
      dockerfile: dashboard/Dockerfile.website
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_SITE_ONLY: "true"
    restart: unless-stopped
